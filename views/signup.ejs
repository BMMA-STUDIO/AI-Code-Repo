<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%-title%></title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Raleway:ital,wght@0,100..900;1,100..900&display=swap');

        body {
            font-family: 'Arial', sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            background-color: #f5f6f8f9;

            margin: 0;
        }

        .form-container {
            background: white;
            padding: 3rem;
            border-radius: 18px;

            box-shadow: 
                -4px 4px 8px rgba(116, 114, 215, 0.1),   /* Purple - lower left */
                -2px 2px 12px rgba(251, 88, 118, 0.1),  /* Pink - middle transition */
                2px -2px 16px rgba(250, 173, 65, 0.10);   /* Orange - upper right */
            
            width: 85%;
            max-width: 330px;
            margin:1rem;
        }

        .form-group {
            margin-bottom: 1.5rem;
        }

        label {
            display: block;
            margin-bottom: 0.5rem;
            color: #333;
            font-size: 0.9rem;
        }

        input {
            width: 100%;
            padding: 0.50rem;
            border: 1px solid #ddd;
            border-radius: 6px;
            font-size: 1rem;
            transition: border-color 0.3s ease;
        }

        input:focus {
            outline: none;
            border-color: #007bff;
            box-shadow: 0 0 5px rgba(0, 123, 255, 0.2);

        
        }

        button {
            width: 100%;
            padding: 0.65rem;
            background: #0f0f0f;
            border: none;
            border-radius: 8px;
            color: white;
            font-size: 1rem;
            cursor: pointer;
            transition: background 0.3s ease;
        }

        button:hover {
            background: #0168dddc;
        }
        h2 {
            color: #1c1c1cfa;
            font-family: "Raleway", sans-serif;
        }
    </style>
</head>
<%-include ('header'); -%>
<body>
    <div class="form-container">
        <h2>Verify Your Profile</h2>
        <form>
            <div class="form-group">
                <label for="name">Social Link</label>
                <input type="text" id="name" name="name" required>
            </div>
            <div class="form-group">
                <label for="email">Email</label>
                <input type="email" id="email" name="email" required>
            </div>
            <div class="form-group">
                <label for="message">Policy</label>
                <input type="text" id="message" name="message" required>
            </div>
            <button type="submit">Verify Me</button>
        </form>
        <h4><a href="/beema">Home</a></h4>
    </div>

</body>
</html>