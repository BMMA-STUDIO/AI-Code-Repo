<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    
<style>
    .dialog-container {
        position: fixed;
        top: 5%;
        left: 5%;
        width: 90%;
        height: 90%;
        border-radius: 9px;
        display: flex;
        justify-content: center;
        align-items: center;
        background: rgba(229, 227, 227, 0.4);
        z-index: 1000;
        box-shadow: 3px 3px -4px 9px rgb(236, 237, 238);

    }
    
    .dialog-box {
        background: #fff;
        border-radius: 20px;
        width: 300px;
        box-shadow: 0 3px 10px rgba(0, 0, 0, 0.2);
        overflow: hidden;
        font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
    }
    
    .dialog-content {
        padding: 20px;
        text-align: center;
    }
    
    .dialog-content h3 {
        margin: 0 0 10px 0;
        font-size: 18px;
        font-weight: 600;
        color: #000;
    }
    
    .dialog-content p {
        margin: 0;
        font-size: 14px;
        color: #666;
    }
    
    .dialog-buttons {
        display: flex;
        border-top: 1px solid #e5e5e5;
    }
    
    .btn {
        flex: 1;
        border: none;
        background: none;
        padding: 12px;
        font-size: 16px;
        font-weight: 500;
        cursor: pointer;
        transition: background 0.2s;
    }
    
    .no-btn {
        color: #007aff;
        border-right: 1px solid #e5e5e5;
    }
    
    .yes-btn {
        color: #007aff;
        font-weight: 600;
    }
    
    .no-btn:hover {
        background: #f5f5f5;
    }
    
    .yes-btn:hover {
        background: #f5f5f5;
    }
    </style>
</head>
<body>
    <div class="dialog-container">
        <div class="dialog-box">
          <div class="dialog-content">
            <h3>Ready to login?</h3>
            <p>This action cannot be undone.</p>
          </div>
          <div class="dialog-buttons">
            <button class="btn no-btn" onclick="window.location.href='/'">No</button>
            <button class="btn yes-btn" onclick="window.location.href='signup'">Yes</button>
          </div>
        </div>
      </div>
</body>
</html>